name: build caller

on:
  push:
    branches: ["main"]

jobs:
  use-build:
    uses: ./.github/workflows/resuable-build.yml
    with:
      app_name: "my-web-app"
      environment: "production"
    secrets:
      docker_token: ${{ secrets.DOCKER_TOKEN }}
