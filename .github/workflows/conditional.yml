name: conditionals

on:
    push:
        branches: [main]

jobs:
    condition:
        name: conditional
        runs-on: ubuntu-latest
        steps:
                - name: when main branch
                  if: true
                  run: |
                        echo "Running due to push to $GITHUB_REF"