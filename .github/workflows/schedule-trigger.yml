name: schedule trigger

on:
    schedule: 
        - cron: '0 9 * * 1'

jobs:
    list-files:
        runs-on: ubuntu-latest

        steps:
            - name: list files
              run: |
                    ls -aR