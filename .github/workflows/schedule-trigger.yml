name: schedule trigger

on:
    schedule: 
        - cron: '0 0 * * *'

jobs:
    list-files:
        runs-on: ubuntu-latest

        steps:
            - name: list files
              run: |
                    ls -aR